# Seam repair verification protocol (not yet executed)

Current owner boundaries: `sprite_sampling` owns `Sprite2D` production and Core/SourceGen contract tests; `village_app` owns the native seam reproduction, Point opt-in for Tiny Town sprites, and Village app/tests; the canonical API documentation owner owns the `Sprite2D` page/manifest. Integration owns only this evidence area, the isolated current Core API baseline, and the eventual serialized cross-project gates. Builds/tests must not overlap across owners.

## Evidence prerequisites

1. Keep the observed native screenshot/crop RED and exact original scenario as facts. The public Point opt-in remains per-sprite where Village requests it; default Linear remains unchanged for other `Sprite2D` consumers. The user-confirmed private image-domain rule applies only to Point + Clamp image draws, not globally. Do not diagnose the backend by visual intuition alone.
2. Wait for Village to release its exclusive focused build/native lease and provide current source hash, RED/GREEN commands/logs/TRX, and app-owned screenshots. Wait for root's shared Astra narrow integrated review before the costly full solution run.
3. Preserve `pre-sampling-api/Cerneala.dll` hash `EC8BEE87C67F0558E85B4D637903AA087C980931EDDE9DD4922B58498774D87B`. Candidate Core DLL must be the actual post-fix build, never a copy of this baseline. Compare with `api-compat-core.proj` using strict mode and no suppressions; classify each exact additive/removed/changed public member. Strict CP0002 for an approved addition may yield exit 1 and is not a breakage verdict by itself.
4. Record the actual owner cost evidence, counters, warmup, scenarios, and limitations. The accepted candidate keeps ordinary 32-byte vertices/shaders and one GPU upload arena, with a selected 64-byte image-domain path, lazy selected CPU storage, and reusable packing only for mixed flushes. The existing native warm run has no matched pre-change native baseline and no GPU timing; do not claim a causal regression/improvement or unchanged performance from design intent. Verify ordinary-only construction does not pay the rejected eager 64 KiB selected-array payload.

## Serialized final ladder after authorization

- Confirm Core affected and SourceGen focused raw evidence from the owning worker, Village original native RED→GREEN and full dedicated native gate from its owner, and canonical documentation/manifest synchronization.
- Run the canonical manifest test `Cerneala.Tests.VisualStudio.Stage6ReleaseHarnessTests.ApiDocumentationManifestIsValidAndReferencesExistingFiles` in `tests/Cerneala.Tests.VisualStudio/Cerneala.Tests.VisualStudio.csproj` after docs freeze, unless the final build-enabled full solution already runs that exact current test and result can be identified in its TRX.
- Run the dedicated shader artifact verifier after any shader edit/regeneration: `dotnet run --project .\Tools\Cerneala.SdlShaderCompiler\Cerneala.SdlShaderCompiler.csproj -c Release --no-restore -- --verify --manifest .\Cerneala.Backends.SdlGpu\Shaders\manifest.json`. The normal incremental backend build's verify target can be skipped by an up-to-date stamp, so do not substitute a successful build for explicit artifact-byte verification.
- Inspect the shader manifest and compiled artifact inventory: the approved selected image-domain vertex/pixel pair would increase the expected shader count from eight to ten. Verify all ten current artifacts if implemented, not merely the count, and confirm ordinary shader bytes/layout routing remain unchanged except where the private image-domain contract requires it. Check candidate public/protected metadata for **no extra renderer API** beyond the separately approved `Sprite2D.Sampling`/`SamplingProperty` addition.
- Before broad conformance, run the owner-provided focused native RED/GREEN matrix for the user-confirmed logical image-domain rule. It must exercise Point + Clamp external boundaries on horizontal and vertical edges, actual runtime DPI/sample-count conditions, flipped and rotated crops, alpha, fractional source rectangles without blanket half-texel shrink, separate selected regions within sprite batches, options-based image quads, and nine-slice internal patch seams versus the whole selected region. It must also preserve the internal-triangle/topology control and show Linear, Wrap, arbitrary-mesh, and surface-composite paths unchanged. Record exact fixture names, commands, pixel observations, captures, and pass/fail counts from the actual owner evidence; these cases are required coverage, **not tests already run or green**.
- Include the later user-approved phase-0.500 threshold amendment in that focused RED/GREEN matrix: an external logical image boundary pixel center **on or outside** the boundary uses a covered position; strictly interior centers and shared internal diagonals keep ordinary interpolation. Do not reuse the earlier 925-case SDL run as final proof after this amendment. Core must revalidate affected SDL/Village native cases and the measured cost gate on the amended candidate before integration full-suite/API/docs gates run.
- Preserve the paired one-ULP boundary assertions in the final native evidence: selected equals ordinary for the strictly interior center, while selected equals covered/isolated for the exterior center. Record measured inputs and pixel comparisons rather than inferring correctness from a rendered screenshot alone; do not describe this as a universal FP32 guarantee.
- Run focused Windows SDL native conformance after the fixture and shader state are coherent: Core `SdlGpuDrawingConformanceTests` and `PrismSdlGpuPixelConformanceTests`; SDL backend `AlphaBlendRenderingTests`, `NativeDrawingSmokeTests`, and `PrismBaselineConformanceMigrationTests`. These cover ordinary image/alpha, clip, transform, and Prism paths. Four historically disabled alpha InlineData rows (content 1, 3, 6, 7) must remain identified as skips, not counted green. These focused runs do not replace the full solution gate.
- Run strict Core ApiCompat against the frozen **post-Scene2D** baseline using `dotnet msbuild .\docs\plans\evidence\2026-09-24-scene-village-seams\integration\api-compat-core.proj -t:CompareCore -v:minimal`; archive raw command/log/exit and exact diagnostic classification.
- Run one build-enabled Release `dotnet test .\Cerneala.slnx -m:1` with `CERNEALA_SDL_NATIVE_TESTS=1`, process-plus-command `DefaultItemExcludesInProjectFolder=artifacts/**`, and unique TRX output under this seam evidence tree. Parse every project TRX and every skip. The prior Village full result (6,318 passed) belongs to the pre-seam candidate and must not be used as post-fix proof.
- Confirm app-owned screenshot API captures, source/output asset hashes, current source/assembly hashes, worktree preservation, `git diff --check`, new-file text integrity, and final regenerated/read `FileTree.md`. Stop all jobs and writers before the shared independent audit. Do not claim human play, physical Alt-Tab, performance, or hosted CI execution.

This is a protocol, not a checked plan or evidence that these gates have run.

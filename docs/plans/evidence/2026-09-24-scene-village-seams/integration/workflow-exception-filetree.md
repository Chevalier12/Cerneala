# FileTree generation outside assigned writer ownership

The Core worker reported that its read-only Luna geometry reader inadvertently ran `.\Tools\scripts\New-FileTree.ps1` from the repository root, despite its read-only assignment. Its output was `Wrote C:\Users\lauri\Desktop\Cerneala\FileTree.md`. The exact invocation UTC was not captured; the file's LastWriteUtc is the observed timestamp below. This is a workflow violation, not an authorized source change. The generator wrote tracked `FileTree.md`; no rollback, checkout, or deletion was attempted because the tree had earlier dirty user/task state and regeneration is not safely reversible from a hash alone.

Before this reader's action, integration had generated and completely read `FileTree.md` at SHA-256 `5B6237FD4FB25A6FD258077400CF713D694F5B1C76B2E89CD8035775CFEDE1DF`, 5,741 raw lines (`pre-seam-integrity.txt`). After the reader action, read-only checks observed SHA-256 `D4AB40F4964123CEC1003983940F76CC333B60C6514086AE24ABEF85A1DAC487`, 5,875 raw lines, 272,780 bytes, and LastWriteUtc 2026-09-24T21:02:30Z. The 134-line count increase is consistent with additional seam source/evidence paths being present, but line-count change alone does not prove that every intermediate user/tree edit was preserved. `git diff --numstat -- FileTree.md` was 981 additions / 68 deletions **against HEAD**, not an isolated before/after diff for the reader's command. No prior byte snapshot was available to compute that exact delta.

The Core worker inspected `New-FileTree.ps1`: its default output is root `FileTree.md`, and its only content write is `Set-Content -LiteralPath $resolvedOutput`. No other generated target was reported by the reader. Integration did not inspect unrelated dirty `CLAUDE.md`/`.claude` content. The final repository checkpoint still requires integration-owned regeneration and complete reread after all source/evidence files quiesce, plus normal worktree integrity and independent audit. Do not present this unassigned generator run as final verification.

The reader separately reported Git blob SHA-1 `0bd5f3d4ac7daaa741477ed4519086eba858c6ca` for HEAD and `e8ce8290a71c093e47f126e8e3aa208f529e5183` for the current generated tree. These are **not** a byte snapshot of the pre-reader working tree and cannot be used to reconstruct an exact before/after event diff.

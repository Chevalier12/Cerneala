# Stage 3 final local integrity and scope record

Date: 2026-09-24. This record applies after the successful build-enabled, Windows SDL native-opt-in full-solution run in `full-slnx-final-isolation/`. After independent candidate acceptance, the source plan's Stage 3 tasks, gate, and definition of done were checked on root authorization; post-edit checklist audit remains pending. The subsequent user decision limits this change's platform gate to Windows SDL; Linux, macOS, and WindowsDX are not validated.

## Final executable evidence and inputs

- `full-slnx-final-isolation/command.txt`, `exit.txt`, `full-slnx-final-isolation.log`, and ten unique TRXs: exit 0; **6,303 passed, 0 failed, 6 skipped, 6,309 total**. The command built the solution in Release with `CERNEALA_SDL_NATIVE_TESTS=1`, serial MSBuild and the already characterized process-local/CLI `DefaultItemExcludesInProjectFolder=artifacts/**` property. A read-only parse matched all ten `Results File` references to distinct TRXs. VisualStudio **47/47** includes the manifest and VSIX payload tests. This is not a claim that skipped behaviors passed.
- `api-compat-core.*` and `api-compat-packages.*` have exit 1 for the intentionally approved 23 Core plus 20 Package CP diagnostics. The exact current diagnostic members, order, and direction match the independently reviewed Stage 2 classification; no additional diagnostic or `TileMapChunkData2D` removal appears. Raw nonzero results are retained, not suppressed or relabeled as exit-zero success.
- `current-input-hashes.txt` records the candidate binary/source hashes. Core SHA-256 `EC8BEE87C67F0558E85B4D637903AA087C980931EDDE9DD4922B58498774D87B`; Packages SHA-256 `DD8790173B27740D1B3819528AE0BA9C5088E3D70B32523D1C247CACC4002DC8`. Those match the accepted Stage 2 cutover binaries; the Stage 3 project-file repair is limited to the VSIX consumer's two private restore paths. Its source hash is recorded there.

## Worktree and temporary-artifact scan

- `final-tracked-diff-names.txt` and `final-untracked-paths.txt` archive the current tracked name/status list and nonignored untracked paths. These are scope inventories, not a claim that every untracked evidence file is already tracked. Stage 0's user-preexisting `.codex` config/skill, importer docs, and `TileMap2DFactoryTests.cs` still match their baseline SHA-256 values; Stage 2's independent audit accepted preservation of the in-scope preexisting `TileMap2D.FromModel` and associated source/docs edits. No reset/checkout or destructive cleanup was performed.
- `final-untracked-temp-candidates.txt` scans every nonignored untracked path for `.csx`, `.tmp`, `.bak`, `.orig`, `.rej`, trailing `~`, `tmp`/`temp`/`scratch` directories, or new `bin`/`obj`/`artifacts` directories. It reports zero matches. `final-ignored-temp-scan.txt` separately scans ignored paths under the Stage 3 evidence tree and VSIX project for those temporary names; it reports zero matches among **1,448** ignored paths. Existing ignored `bin`/`obj` build outputs and old WindowsDX residue are not treated as source or native validation and were not deleted. These scoped scans do not prove absence of arbitrary files elsewhere on disk.
- `core-compile-items-baseline.json`, `core-compile-items-excluded.json`, and `core-compile-items-diff.txt` show the command-only artifact exclusion removed exactly 17 generated `artifacts/` C# Compile inputs, 1,111 to 1,094, with zero intended source removed or added. The same property was exported to the full-run process so test-launched child builds inherited it. No Core project setting was changed to mask the contamination.
- `final-git-diff-check.txt` and `.exit.txt` record `git diff --check` exit **0** across the tracked change set. `final-markdown-mechanical.txt` records zero trailing-whitespace or conflict-marker findings in the source plan, Stage 3 ledger, platform addendum, and this untracked record. `final-filetree-hash.txt` records the regenerated `FileTree.md` hash. These mechanical checks are not a semantic audit of the integrated source, tests, documentation, and platform decision.

## Remaining human/audit boundary

The six final-suite skips are individually explained in `stage3-verification.md`: a disabled Language completion P95 gate, a disabled Windows foreground-focus native ownership case, and four disabled alpha-blend parameter rows. The performance behavior, foreground-focus case, and four alpha cases are **not** validated. The original driver explanation for the alpha rows is reported, not established as root cause. No manual human interaction was performed or claimed. The shared independent auditor accepted the integrated candidate and root authorized checkpointing; the edited plan/checkpoint still requires its separate independent audit before goal completion.

# Stage 3 verification ledger (open)

Date: 2026-09-24. Source plan: `docs/plans/2026-09-23-scene2d-simple-collections-and-internal-spatial.md`, lines 111–134. After the independent Stage 3 candidate audit and root-authorized checkpoint, the five Stage 3 tasks, its gate, and all four definition-of-done items are checked. The post-edit checkpoint audit remains pending. Source-plan SHA-256: `3A000DB978121269BD3B1A5E61C092317DDA445A6BA384488CF5CEEE07E24EA9`. This ledger distinguishes executed checks, investigated skips, and platforms explicitly excluded from this change's gate.

**Latest local result:** the final build-enabled, native-opt-in `dotnet test .\Cerneala.slnx` run in `integration/full-slnx-final-isolation/` exited **0** with **6,303 passed, 0 failed, 6 skipped** across ten test projects. This supersedes the failed full-solution attempts retained below. The user subsequently narrowed this change's platform gates to **Windows SDL only**; Linux, macOS and WindowsDX were **not** tested and are explicitly excluded, not counted green. The six skips are reported individually below and are not treated as passes. The source-plan checklist was checked only after independent candidate acceptance and root authorization; its durable post-edit audit is still pending.

## Executed local checks so far

- The owning SceneItems worker added deterministic idle/delta checks and recorded narrow **2/2** and owned classes **43/43** passing, with 16-frame counters and raw output in `sceneitems/verification.md`. No Stage 3 RED phase or production fix is claimed: these new checks passed against the existing implementation. Its build/test lease was released; no production source changed in that pass.
- `core-focused.command.txt`, `.log`, `.trx`, `.exit.txt`: the plan-exact focused Core filter, Release with the command-only artifact exclusion, passed **328/328**, 0 failed, 0 skipped, exit 0. This built the current Core test input including the new deterministic tests.
- `importers.command.txt`, `.log`, `.trx`, `.exit.txt`: the current Core-dependent Scene2DImporters project passed **173/173**, 0 failed, 0 skipped, exit 0.
- The native worker's `native-windows/native-migration/run-ledger.md` records opt-in Windows SDL execution: new Tetris/package lifecycle cases **2/2** and the plan-named warm/grid/Prism cases **6/6**, no skips. The new lifecycle test was subsequently edited for setup-failure cleanup. `integration/package-lifecycle-final-fixture.*` rebuilt its final source and reran that single lifecycle case **1/1**, no skip, exit 0; the first 2/2 is historical for the old fixture hash.
- **Historical failed run, superseded below:** `integration/sdlgpu-all-native.*` ran the entire SDL test project with `CERNEALA_SDL_NATIVE_TESTS=1` and `--no-build`: **868 passed, 26 failed, 5 skipped**, exit 1. It ran the previously built test DLL while the lifecycle test source was being repaired, so its lifecycle case cannot verify the final fixture. Raw TRX classification: 25 parameterized `NativeScenePrismDomainTests` failures all assert expected 9, actual 0 at `Run` line 240; one `NativeImageLeaseTests.GridCameraInputRetiresAtlasesAndKeepsNpcTerrainAcross32Cycles` fails expected 2, actual 1 at line 90. The five skips are one maintainer-disabled native foreground-focus setup case and four user-disabled alpha-blend cases reported on NVIDIA; the recorded skip text says the latter also fail on a clean historical commit and does not independently establish driver causation. None of the 26 failures is in new Tetris/lifecycle or the plan-named six native cases. The failure was diagnosed and the affected fixture contracts were repaired and rerun below; it is not the current broad result.
- This initial check did not exercise Linux/macOS, WindowsDX, the complete solution, or final current API/documentation comparison. The later local results below supersede the latter two omissions; Linux/macOS and WindowsDX remain open.

### Historical broad native failure diagnosis and verified fixture correction

The 25 Prism-domain failures occur **before pixel comparison**. A focused instrumented RED found the failing source is the active terrain branch while the assertion inspects the detached actors `SceneItems2D` (`RealizedItemCount=0`, context null); its original source still contains nine entries. The migrated fixture unconditionally checked actor membership, whereas the historical fixture selected actor count or map drawn-tile count by active source. The selected lifecycle contract retires detached realizations. Root authorized a source-specific assertion that retains the actor/terrain checks, not a relaxed expected count.

The image-lease failure occurs **before its 32-cycle loop** after initial screenshot/color and image-residency assertions pass. The focused native observation found active simulated NPC geometry x=1984..2000, one near map collider adapter x=16..32, three map chunks and one visible chunk; the far collider at x=2016 is not pinned by that current geometry. Historical metadata had wider interest x=1980..2044, but that public pre-load spatial catalog was deliberately removed. Root authorized the native fixture to hold an explicit movement-owned `PrepareRegionAsync` region for the upcoming far-wall sweep, preserving the expected far adapter/blocked movement/cache-cycle assertions. Neither finding establishes a production renderer or streaming defect. Both fixture corrections were then tested narrowly and in the current broad SDL rerun below; the failed 899-test result remains archived as RED evidence.

### Current Windows native and full-solution gate

The native owner archived repaired focused **2/2** and affected Prism-domain/image-lease **49/49** passing with no skips in `native-windows/native-migration/fixture-repair-ledger.md`. Current rebuilt `integration/sdlgpu-all-native-after-fixtures.*` then passed **894**, failed 0, skipped 5, total 899, exit 0. The five skips remain the historical disabled foreground-focus/alpha-blend cases listed above; this is not evidence those cases passed. Current opt-in Core SDL conformance `integration/core-sdl-conformance-native.*` passed **133/133**, 0 skips. Canonical VisualStudio manifest test `integration/manifest.*` passed **1/1**. No production source changed for these fixture repairs.

The Stage 3 strict Core and separate Package ApiCompat raw logs are `integration/api-compat-core.*` and `integration/api-compat-packages.*`. Both commands exit 1 for the intentional selected cutover. A read-only exact diagnostic comparison found **23 Core + 20 Package** CP diagnostics identical in order/member/direction to the 43 individually reviewed Stage 2 rows; no new, deleted, or changed CP diagnostic and no `TileMapChunkData2D` break. These are classified expected incompatibilities, not green command exits or suppression-file waivers.

**Historical failed run, superseded below:** `integration/full-slnx/command.txt` invoked `dotnet test .\Cerneala.slnx` in Release with `CERNEALA_SDL_NATIVE_TESTS=1`, serial MSBuild, and the command-only artifact exclusion. The raw `full-slnx.log` and `exit.txt` show **exit 1**. Green portions include Language 234 with one historical P95 skip, PreviewHost 14, Importers 173, Packages 104, SDL 894 with five disabled skips, SourceGen 610, Core 4,156, and Tetris 31. LanguageServer had two failing test cases (38 pass) with generated `artifacts/` C# leaking into test-launched temporary workspace builds despite the outer exclusion. The VisualStudio project encountered NETSDK1047 for a `net10.0/win-x64` LanguageServer assets target in its bundled-server build, followed by MSB4181. Those failures were diagnosed and addressed separately below; this first command is not current-state verification.

The nested LanguageServer commands omit the outer command-line MSBuild property. `integration/core-compile-items-baseline.json`, `core-compile-items-excluded.json`, and `core-compile-items-diff.txt` establish the property removes exactly **17** `artifacts/` generated C# inputs from the Core Compile item set (1,111→1,094), with zero non-artifact removals or added items. A process-local environment export of the same property reaches test-launched child builds: `integration/language-server-structure-env-probe.*` passed 1/1; then `integration/language-server-env-full.*` passed 40/40, 0 skipped. No project/config/source was edited for this correction. It explains the observed test fixture compilation failures, not the separate VisualStudio RID-assets failure.

`integration/visualstudio-targeted.*` built and published the extension in direct-project context and passed the manifest test 1/1, with a binlog. A second **historical** solution-context attempt, `integration/full-slnx-current/*`, inherited the artifact-exclusion environment and rebuilt the full solution with native opt-in: all recorded test projects passed (Language 234 + one P95 skip; LanguageServer 40; PreviewHost 14; Importers 173; Packages 104; SDL 894 + five disabled skips; SourceGen 610; Core 4,156; Tetris 31), but the VisualStudio project **again failed before test execution** with NETSDK1047/MSB4181. It produced no VisualStudio TRX and the command exited 1. The restored project assets *after* that run include the RID target; this does not prove what ResolvePackageAssets read at failure time. A filtered solution-context build in `integration/full-slnx-vs-filter/*` reproduced the same VisualStudio failure quickly while selecting only the manifest test. Its failed binlog and the direct success binlog have identical explicit nested `Restore;Publish`/`RuntimeIdentifier=win-x64` parameters, but the solution child inherits additional solution globals. A direct VS probe with only `BuildingSolutionFile=true` in `integration/vs-global-probe/*` passed 1/1, falsifying that property alone as cause. The bounded diagnosis and consumer-owned repair that superseded this failure are recorded below.

Binlog comparison narrowed that discrepancy: the failed nested LanguageServer `RestoreGraphItems` included `RuntimeIdentifiers=;win-x64` and `TargetFrameworks=net10.0`; `ResolvePackageAssets` received the correct assets path, `TargetFramework=net10.0`, and `RuntimeIdentifier=win-x64`. In the failed same-project context, `RestoreTask` logged writing `Cerneala.LanguageServer/obj/project.assets.json` at local 13:56:46.4259 and `ResolvePackageAssets` reported NETSDK1047 at 13:56:46.5035. The file snapshot after that run contained both `net10.0` and `net10.0/win-x64`, and was not a failure-time content capture. In direct success, the nested restore did not rewrite LanguageServer assets, and resolution succeeded with the same task inputs. At that point the cache explanation was only a hypothesis; subsequent source/task-cache and solution-build evidence, followed by the targeted repair and current full rerun, are recorded below.

### VSIX restore-asset ownership repair and final local verification

The later binlog established an ordered same-build chain on one MSBuild node: solution Restore wrote RID-neutral LanguageServer assets, the solution build resolved those assets, then the VSIX's nested `Restore;Publish` rewrote the **same path** with `win-x64` and `ResolvePackageAssets` reported the RID target missing. Installed .NET SDK 10.0.400 task assembly `Microsoft.NET.Build.Tasks.dll` (file version 10.0.14.38015) uses a `LockFileCache` registered for Build lifetime and keyed by the assets **path**, without mtime/RID/content in its key. A stale cache hit is strongly supported by the sequence but is not directly logged. A solution `Build` without top-level Restore succeeded against already RID-inclusive assets, a discriminator; the first attempt at that probe used invalid `Platform=AnyCPU` and failed MSB4126, then the corrected probe omitted that value and built all projects. Both raw attempts are under `integration/solution-build-no-top-restore/`; neither replaces the full-suite gate.

The architecture owner is the VSIX's two consumer-specific self-contained `win-x64` bundles, not the documented cross-platform LanguageServer. In `Cerneala.VisualStudio/Cerneala.VisualStudio.csproj`, each nested `Restore;Publish` now receives the SDK 10.0.400 exact-project-path `ProjectToOverrideProjectExtensionsPath` and `ProjectExtensionsPathForSpecifiedProject` pair, pointing to a **distinct sibling** of its publish directory under the VSIX `obj` tree. No producer project, public API, SDK cache, user artifact, or global build configuration was changed. First isolating LanguageServer moved the faithful filtered solution RED to the analogous PreviewHost RID-assets failure; isolating PreviewHost separately made the **same filtered solution-context build and manifest test GREEN**, exit 0 (`integration/full-slnx-vs-filter-isolated-both/`). The previous RED and partial failure are archived in `integration/full-slnx-vs-filter/` and `...-isolated/`.

The final filtered binlog shows both nested Restore and Publish succeed with different assets paths: `Cerneala.VisualStudio/obj/Release/net472/bundled-server-restore/project.assets.json` has `net10.0` plus `net10.0/win-x64`; `.../bundled-preview-host-restore/project.assets.json` has `net10.0-windows` plus `net10.0-windows/win-x64`. Root LanguageServer and PreviewHost assets remain framework-only; the `Cerneala.Language` project reference uses its own default `obj/project.assets.json`. The VSIX contains versioned Server and PreviewHost exe/dll/coreclr payloads; the dedicated `VisualStudioPackageTests.VsixContainsHostServerGrammarConfigurationAndAssets` test passed 1/1 (`integration/vsix-contents-after-isolation.*`). The canonical manifest test also passed in the filtered solution run.

The **final** `integration/full-slnx-final-isolation/command.txt` ran `dotnet test .\Cerneala.slnx` in Release with **build enabled**, `-m:1`, `CERNEALA_SDL_NATIVE_TESTS=1`, and `DefaultItemExcludesInProjectFolder=artifacts/**` both as process environment (for nested fixture builds) and command-only property. `exit.txt=0`; its ten unique TRXs and raw log give: Language 234 passed/one historical P95 skip; LanguageServer 40; PreviewHost 14; Importers 173; Packages 104; SDL_GPU 894 passed/five historical disabled skips; SourceGen 610; VisualStudio 47 (including API manifest and VSIX contents); Core 4,156; Tetris 31. Aggregate **6,303 passed, six skipped, zero failed, 6,309 total**. The 361 SDL opt-in cases in that TRX are 357 passed and four explicitly disabled alpha-blend theory cases skipped; no case was skipped for an unset native opt-in. The six skips are: one maintainer-disabled Language completion CPU P95 gate (underlying performance issue unresolved), one maintainer-disabled Windows foreground-focus ownership/input/lifetime case, and four user-disabled NVIDIA-reported alpha-blend cases that the source says also fail on a clean historical commit; the driver cause is not independently established. These disabled behaviors were **not** validated by the final suite. No human manual validation is claimed.

Final current-input, worktree, temporary-artifact, and checklist-scope checks are recorded in `integration/final-integrity.md` with raw path lists and mechanical outputs. They do not substitute for the independent integrated audit or turn the six skips into passes.

## Serialized execution order

Only one worker owns build/test outputs at a time. Do not run against inputs that another writer is changing. Archive each exact command, exit code, full raw output, TRX when applicable, pass/fail/skip counts, skip reasons, and relevant input hashes. Use Release and the evidence-backed command-only `'-p:DefaultItemExcludesInProjectFolder=artifacts/**'` property to exclude generated `artifacts/` C# files; do not change a project file to bypass them. Stage 1 established that the exclusion removes 17 artifact files and no intended source. Reuse a green current-input run; a source/project change invalidates dependent evidence.

1. **Deterministic cost/collection contract.** `core_red` owns the Stage 3 idle/layout/invalidation regression additions and initial focused build/test lease. Establish 16-frame idle and observable-delta assertions with existing `FrameStats`, invalidation trace, enumeration/membership, and collision mutation measurements. Do not infer a zero-work claim from a green Stage 2 test that did not measure those quantities. Preserve the selected `IEnumerable` contract; there is no old 10,000-entry viewport-only target.
2. **Windows SDL native reproduction, targeted first.** After Core and SDL test sources are quiescent, run with `CERNEALA_SDL_NATIVE_TESTS=1` and the real `[SdlNativeFact]` opt-in: `NativePackageWarmStreamingTests`, `NativePackageGridSubdivisionTests`, `NativeScenePrismStreamingTests`, and the new Tetris native fixture if its bounded implementation is accepted. Capture package cancellation/retry/release, map pan/zoom/subdivision, Prism, input/collision, template/cache, and Tetris observations with test-owned assertions. Any Cerneala application screenshot must use `Window.SaveScreenshot`; a build or skipped native test is not execution evidence. Run the failed subset before broadening if needed.
3. **Broader SDL native and affected suites.** With the opt-in still set, run the relevant native SDL project suite, recording actual pass/fail/skip and resource/driver blockers. Then run the plan's focused Core filter (`SceneItems2D|SceneSpatial|TileMap|Collision`) and affected SourceGen, Scene2DPackages, Scene2DImporters, SdlGpu, and Tetris test projects; use migrated-contract filters where applicable. Include current Language results in the final solution run because the Stage 2 Language suite was not rerun against the last Core DLL. Do not present the Stage 2 SDL compile as a native pass.
4. **Documentation, API, and complete solution.** Run `Stage6ReleaseHarnessTests.ApiDocumentationManifestIsValidAndReferencesExistingFiles` in `tests/Cerneala.Tests.VisualStudio`, then the frozen-baseline strict Core and separate strict Package ApiCompat comparisons. Their Stage 2 reviewed 23+20 intentional CP diagnostics cause exit 1; success here means every current exact member is accounted for against the reviewed classification and no new diagnostic appears, not an exit-zero fiction. Then run `dotnet test .\Cerneala.slnx` in Release against the final source state, with the command-only artifact exclusion, serial MSBuild (`-m:1`), raw output and TRX. Investigate each failure and relevant skip; do not use a stale `--no-build` binary.
5. **Final local integrity/conformance.** Run `git diff --check`, inspect current source/test/documentation/API and temporary artifacts, and rerun only evidence invalidated by a late relevant edit. Keep every required native/platform gate open until executable evidence or a new explicit user waiver exists. Human validation is separate and cannot be claimed from automation.

The exact Stage 2 command precedents are `docs/plans/evidence/2026-09-23-scene2d-stage2/integration/*-final-repair.command.txt`, `api-compat-review.md`, and `api-compat-exact-classification.tsv`. The strict Core command is `dotnet msbuild .\docs\plans\evidence\2026-09-23-scene2d-stage1\api-compat.proj -t:Compare -v:minimal`; the separate Package command is `dotnet msbuild .\docs\plans\evidence\2026-09-23-scene2d-stage2\integration\api-compat-packages.proj -t:ComparePackages -v:minimal`. The actual Stage 3 commands, raw exit codes, and diagnostic classification are archived as `integration/api-compat-core.*` and `integration/api-compat-packages.*` and summarized above.

## Platform availability, read-only inventory

The Stage 0 platform matrix (`docs/plans/evidence/2026-09-23-scene2d-stage0/contract-and-platform-matrix.md`, lines 39–49) originally required Windows, Linux, and macOS SDL_GPU native gates plus WindowsDX unless newly waived. That was the **historical requirement before the explicit 2026-09-24 user scope revision** recorded below and in source-plan section 10. `.github/workflows/desktop-backends.yml` configures Windows/Ubuntu/macOS SDL_GPU jobs, including `CERNEALA_SDL_NATIVE_TESTS=1`; configuration is not a run. No CI job was dispatched for this task.

Read-only local checks on 2026-09-24 found Windows `win-x64` with .NET SDK 10.0.400, Intel UHD Graphics 630 and NVIDIA RTX 2060. `CERNEALA_SDL_NATIVE_TESTS` was unset before Stage 3. `wsl.exe --list --verbose` exited 1 and reported WSL not installed. No `docker`, `podman`, `qemu`, or `xvfb-run` command was found; no `sw_vers`, `xcodebuild`, or `xcrun` command was found. Thus this host does not supply a runnable Linux/Xvfb or macOS lane. Linux/macOS native checks remain **unexecuted**, not skipped by approval.

No current tracked `Cerneala.Backends.MonoGame`, `Cerneala.Platforms.Win32`, or `tests/Cerneala.WindowsDxSmoke/Cerneala.WindowsDxSmoke.csproj` exists; the current workflow has no WindowsDX lane. `docs/audits/2026-09-05-monogame-removal.md` records removal of the production backend and active consumers. Ignored September 5 `tests/Cerneala.WindowsDxSmoke/bin` and `obj` binaries are stale residue without current source/harness and were not executed. The user then excluded WindowsDX from this change's gates; absence of a harness is not reported as a green result.

## Platform scope decision after the local inventory

On 2026-09-24 the user explicitly said: “Nu avem cum sa testam Linux si MacOs pe statia asta. Toate verificarile/gateurile raman doar la nivel de Windows”, followed by “Scoate WindowsDX; gate-urile sunt Windows SDL”. Source-plan section 10 records the same limited supersession. Thus **Windows SDL_GPU native with `CERNEALA_SDL_NATIVE_TESTS=1` is the only remaining platform gate for this change**. The Windows opt-in run is archived above. Linux, macOS and WindowsDX have no execution result and are excluded from scope, not validated or silently waived by agent inference. The six disabled cases in the final Windows solution run remain separately reported as skips, not passes.

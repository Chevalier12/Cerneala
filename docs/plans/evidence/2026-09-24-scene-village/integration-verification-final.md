# Scene Village final integration verification — 2026-09-24

This is the current repaired-source handoff. `integration-verification.md` and `full-slnx/` describe an earlier, pre-audit candidate (6,316 passed) and are retained as history, **not** final acceptance evidence. The sole Village source writer froze the 21 app/test/asset files in `app/mirror-native-01-source.sha256.txt`; all 21 hashes were rechecked before the final solution run. `village-source-file-hashes-final.txt` records those same current hashes plus the unchanged solution and Windows workflow.

## Build-enabled solution gate

The exact current invocation is `full-slnx-final/command.txt`: Release `dotnet test .\Cerneala.slnx -m:1` with `CERNEALA_SDL_NATIVE_TESTS=1`, inherited and explicit `DefaultItemExcludesInProjectFolder=artifacts/**`, and 11 TRX files written to `full-slnx-final/`. The command-only artifact exclusion avoids existing ignored generated C# under `artifacts/rendersurface3d`; no project/source workaround or deletion was used.

`full-slnx-final/exit.txt` is **0**. The raw `full-slnx-final/full-slnx-final.log`, 11 TRXs, `trx-summary.csv`, and `trx-integrity.txt` agree: **6,318 passed, 0 failed, 6 skipped, 6,324 total**. Every TRX has one unique log reference, none is missing or unreferenced, and no result has an unclassified outcome. The current Village project passed **15/15**, including its real Windows SDL_GPU native test with no Village skip. `trx-skips.csv` records the six exact existing disabled cases and reasons: one Language completion CPU P95 gate, one native foreground-focus/lifetime case, and four alpha-blend cases with a reported NVIDIA issue. Those six behaviors were not validated.

The earlier focused repairs and RED/GREEN evidence remain in `app/verification.md`: complete tree art, real character walk/facing frames with approved left mirror, two-row toolbar reachability at 820×600, and an actual Servo click on `0` after 10,000. Its current focused native `mirror-native-01` passed **15/15** before this full build; no extra partial suite was run after the accepted repair audit. The native scenario drives Servo-routed input, collision, camera, all three 10,000-realized presets, facing transitions, and zero retirement. It uses `Servo.SaveScreenshotAsync` / `Window.SaveScreenshot`, not an OS capture or direct model property assignments masquerading as input.

## Current output and captures

The built launcher exists at `C:\Users\lauri\Desktop\Cerneala\Playground\Cerneala.SceneVillage\bin\Release\net8.0-windows\Cerneala.SceneVillage.exe`. `binary-hashes-final.csv` records its SHA-256 and the current Village DLL. The Core and Scene2D Packages DLL hashes remain identical to the previously reviewed strict ApiCompat inputs; Village did not change their public surfaces. The current README's workspace-safe build/launch command sets the artifact exclusion, builds Release, and runs with `--no-build`. The native test launched the actual generated window; no separate interactive `dotnet run` or human play session is claimed.

The final solution run produced **11** app-owned captures at `C:\Users\lauri\Desktop\Cerneala\artifacts\ci\scene-village\screenshots\native-20260924-173644031\`: `village.png`, `player-up.png`, `player-left.png`, `player-right.png`, `player-down.png`, `player-right-after-left.png`, `player-down-after-left.png`, `static-10000.png`, `animated-10000.png`, `collision-10000.png`, and `cleared-0.png`. `final-run-screenshot-hashes.csv` records each size and hash. The village, left/right/down transition, and static/animated examples were opened for visual inspection in integration; this is not a manual gameplay or pixel-conformance claim. The earlier accepted dedicated native run has its own 11 captures and hashes in `app/mirror-native-01-screenshots.sha256.txt`.

`built-asset-copy-hashes-final.csv` verifies byte-for-byte output copies of the current `ch003.png`, `tiny-town.png`, license text, and `CREDITS.md`. `asset-output-inventory-final.txt` records exactly these four source/output asset names and **no old `villager.png`** in either source or built output. The two PNGs are credited CC0 in the app's current credits. Ten thousand means realized `SceneItems2D` occurrences, not 10,000 visible draws; no FPS, frame-time, CPU, allocation, or GPU performance target was measured.

## Integration, scope, and cleanup limits

`Cerneala.slnx` still has only the two Village additions. `.github/workflows/scene-village-windows.yml` is the separate path-filtered Windows-only job; its structural check is in `workflow-static-check.txt`. A local YAML parser/actionlint was unavailable and the hosted workflow was **not** dispatched, so hosted Windows native availability is unverified. No Linux, macOS, or WindowsDX run is claimed for this Windows SDL task.

The pre-Village dirty baseline is preserved. Later unrelated `CLAUDE.md`, `.claude/skills/**`, and `claude-skills-import/**` changes were not read for content, edited, reverted, or attributed to Village; the final worktree delta report states their count. `git diff --check`, the current new-file text scan, source hashes, and final regenerated `FileTree.md` are recorded separately. No old completed checklist plan, Git history, remote CI, or pull request was changed by this integration step.

Six **external** inspection artifacts created by the app owner remain under `C:\Users\lauri\AppData\Local\Temp\`: `scene-village-human-rpg-inspect-20260924` (directory, 20 GIFs), `scene-village-belohlavek-ch003-20260924.png` (SHA-256 `02AC85F7A6DD90A486ED4126CC1CE80D482F6D9D17C02AD42DBAAC1C80B479DB`), `scene-village-belohlavek-contact-20260924.png` (`C497589B8D286FC7F77733B9CDF76B41A00C621CF9599F0624955FA2741C109B`), `scene-village-human-rpg-20260924.zip` (`41989301D5A6B2797141A1F1B7963EDCBE325384E7CF27F305529E1B79B02E1D`), `scene-village-human-sheet-inspect-20260924.png` (`FBA4480777128EBA3C40F7776087E77AE33AC5235F402DA0D5B1F28306D63FF9`), and `scene-village-human-walk-frames-inspect-20260924.png` (`7AB8537625333871CCE68E8C744318D4EED5327DE9ED51B2E20A6559D299C891`). The owner reports a path-validated `Remove-Item` cleanup attempt was rejected by the execution tool policy at CreateProcess **before execution**. Integration did not retry or bypass it and did not inspect those external files. They are not source/runtime/build-output assets. Their cleanup remains unverified.

Physical OS Alt-Tab focus loss, a human play session, a performance acceptance threshold, and hosted CI execution remain unverified. The app's deterministic key-clear method is tested, but that is not a physical focus-switch test.

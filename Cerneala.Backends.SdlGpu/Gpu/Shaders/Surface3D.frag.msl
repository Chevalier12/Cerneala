#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct main0_out
{
    float4 out_var_SV_Target0 [[color(0)]];
    uint gl_SampleMask [[sample_mask]];
};

struct main0_in
{
    float2 in_var_TEXCOORD0 [[user(locn0), center_no_perspective]];
    float2 in_var_TEXCOORD1 [[user(locn1), flat]];
    float4 in_var_TEXCOORD2 [[user(locn2), flat]];
    float in_var_TEXCOORD3 [[user(locn3), flat]];
    float in_var_TEXCOORD4 [[user(locn4), flat]];
};

fragment main0_out main0(main0_in in [[stage_in]])
{
    main0_out out = {};
    float _64;
    if (in.in_var_TEXCOORD3 > 0.5)
    {
        _64 = length(in.in_var_TEXCOORD0) - in.in_var_TEXCOORD1.y;
    }
    else
    {
        float _49 = in.in_var_TEXCOORD1.x * 0.5;
        float2 _56 = abs(in.in_var_TEXCOORD0 - float2(_49, 0.0)) - float2(_49, in.in_var_TEXCOORD1.y);
        _64 = length(precise::max(_56, float2(0.0))) + precise::min(precise::max(_56.x, _56.y), 0.0);
    }
    float _66 = fast::clamp(0.5 - _64, 0.0, 1.0);
    if (_66 <= 0.0)
    {
        discard_fragment();
    }
    out.out_var_SV_Target0 = float4(in.in_var_TEXCOORD2.xyz, 1.0);
    out.gl_SampleMask = (1u << (uint(rint(_66 * in.in_var_TEXCOORD4)) & 31u)) - 1u;
    return out;
}

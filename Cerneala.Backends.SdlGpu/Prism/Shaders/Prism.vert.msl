#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct type_PrismVertexUniforms
{
    float2 ViewportSize;
    float2 Padding;
    float4 Destination;
};

struct main0_out
{
    float4 out_var_TEXCOORD0 [[user(locn0)]];
    float2 out_var_TEXCOORD1 [[user(locn1)]];
    float4 gl_Position [[position]];
};

vertex main0_out main0(constant type_PrismVertexUniforms& PrismVertexUniforms [[buffer(0)]], uint gl_VertexIndex [[vertex_id]])
{
    main0_out out = {};
    float2 _43;
    if (gl_VertexIndex == 0u)
    {
        _43 = float2(0.0);
    }
    else
    {
        _43 = select(float2(0.0, 2.0), float2(2.0, 0.0), bool2(gl_VertexIndex == 1u));
    }
    float2 _52 = (PrismVertexUniforms.Destination.xy + (_43 * PrismVertexUniforms.Destination.zw)) / PrismVertexUniforms.ViewportSize;
    out.gl_Position = float4((_52.x * 2.0) - 1.0, 1.0 - (_52.y * 2.0), 0.0, 1.0);
    out.out_var_TEXCOORD0 = float4(1.0);
    out.out_var_TEXCOORD1 = _43;
    return out;
}


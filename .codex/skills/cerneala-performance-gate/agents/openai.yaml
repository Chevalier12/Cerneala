interface:
  display_name: "CernealaPerformanceGate"
  short_description: "Make measured Cerneala performance gates pass"
  default_prompt: "Use $cerneala-performance-gate to reproduce this observable lag through Servo when UI input is involved, inspect the gate through Detective and required external tools, repair any confirmed Servo or Detective defect encountered without stopping for separate permission, add only justified permanent Detective diagnostics for reusable observability gaps, fix the owning Cerneala bottleneck without fidelity loss, and prove my unchanged gate passes."
